<div class="tab-pane fade <% if(typeof(pane) !== 'undefined' && pane == 'whs') { %>show active<% } %>" id="warehouse-pane"
    role="tabpanel" aria-labelledby="warehouse-tab">
    <h2 class="pane-h2">Warehouse Configuration</h2>
    <section class="standard-form-section">
        <form id="warehouse-form" class="standard-form" autocomplete="off" action="/admin" method="POST" oninput=""
            novalidate>
            <fieldset <% if(typeof(disabledForms) !== 'undefined' && disabledForms.orders) { %> disabled="disabled"
                <% } %>>
                <input type="hidden" name="id" value="whs">

                <input type="hidden" name="uid"
                    value="<% if(typeof(user.id) !== 'undefined') { %><%= user.id %><% } %>">
                <% if(typeof(csrfToken) !== 'undefined') { %><input type="hidden" name="_csrf" value="<%= csrfToken %>"><% } %>
            </fieldset>
        </form>
    </section>
</div>